# Copie ce fichier en .streamlit/secrets.toml localement, et colle son contenu
# dans Streamlit Cloud → Settings → Secrets pour le déploiement en ligne.
#
# Le passphrase est demandé au démarrage de l'app (cf. _gate_password()).

[auth]
passphrase = "change-moi-avant-deploiement"
